# LaTeX report source

Run `./build.sh` to compile `report.tex`. The source uses figures from `../figures/` and generated table fragments from `tables/`.

The report uses one self-contained beginner-readable methodology narrative. It distinguishes the text-based morphology index from UD-dependent structural predictors.
