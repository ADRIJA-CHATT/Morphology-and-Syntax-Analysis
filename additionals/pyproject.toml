[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "information-locality-hf"
version = "1.0.0"
description = "Cross-linguistic information locality using Hugging Face FLEURS and Universal Dependencies"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}
